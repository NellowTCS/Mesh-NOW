name: 'Build All Targets'
description: 'Build firmware for all ESP32 targets'

runs:
  using: 'composite'
  steps:
    - name: Build all targets
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.5.1
        command: python scripts/build_all.py --ci --with-frontend